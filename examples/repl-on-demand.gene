#!/usr/bin/env gene

# (repl) to open a repl at the current location
# if some global option is set, open repl on exception

(var a 1)
(var b 2)
(println "Check a and b in REPL.")
(println "Expect `c` to be defined by the REPL.")
(println "And `d` will have the last value from REPL.")
(var d (repl))
(println)
(println "a + b =" (a + b))
(println "c =" c)
(println "d =" d)
