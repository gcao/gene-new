#!/usr/bin/env gene

# (var start (gene/now))
# (var elapsed ((gene/now) - start))

# (println "Execution time: " elapsed " seconds.")

(var start (gene/now))
(gene/sleep 3000)
(var elapsed start/.elapsed)

(println "Execution time: " elapsed " seconds.")
